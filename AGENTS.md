# AGENTS.md

このリポジトリで作業するエージェント（Cursor worker / Claude Code / Cloud Agent）向けの規約。
`CLAUDE.md` はプロジェクトの技術的な説明、このファイルは**情報の取り扱い規約**を定める。

## このサイトは公開されている

`foxtrotdesign.dev` は Vercel でデプロイされた**公開サイト**であり、`/api/chat` を通じて
第三者が内容を問い合わせできる。リポジトリにコミットした時点で GitHub にも載る。
**「コミットする」と「世界に公開する」は同義**として扱うこと。

## 参照してよい非公開ソース

判断・執筆の材料として、以下を**読むことができる**。

| パス | 内容 |
|---|---|
| `/srv/silverbullet` | 85memo（副業・85-Store）のメモ |
| `/srv/silverbullet-nachi` | nachimemo（本業）のメモ |

どちらも読み取り可能だが、**これらは非公開情報である**。
記事の切り口を考える、文脈を理解する、優先順位を判断する、といった用途に使う。

## 絶対に公開しないもの

以下を `content/`、`src/`、`public/` に書き込んではならない。
要約・言い換え・伏せ字にしても、**特定できる形であれば同じく禁止**。

1. **勤務先および取引先に関する一切の情報**
   企業名、案件名、製品型番、担当者名、打ち合わせの内容、社内システム名。
   nachimemo の内容は原則すべてこれに該当する。
2. **未公開の知的財産**
   特許の出願・申請に関する記述、開発中の技術の詳細。
3. **経営数値**
   売上・利益・客単価・来店数・購入率などの実数値および目標値。
4. **個人情報**
   本人以外の氏名、メールアドレス、電話番号、住所。
5. **認証情報**
   APIキー、トークン、パスワード。`.env` の中身。

## 公開してよいもの

- 本人の技術的な試行錯誤、学んだこと、考えたこと
- 公開済みの成果物（GitHub の公開リポジトリ、note.com の公開記事、Bluesky の公開投稿）
- 個人プロジェクト（85-Store の商品・キャンペーンなど、すでに対外的に出している範囲）
- 抽象化された知見（「ある製造業の案件で」ではなく「センサのキャリブレーションで」のように、
  **対象を特定できない水準まで一般化されたもの**）

判断に迷ったら公開しない。迷ったという時点で、その情報は境界上にある。

## 記事を書くときの手順

1. メモを読んで材料にするのは構わない
2. ただし**書くのは、本人の思考・学び・一般化された知見のみ**
3. 書き終えたら必ず次を実行する

   ```bash
   npm run check:disclosure
   ```

4. 検査に通らない限りコミットしない

## 自動チェックについて

`scripts/check-disclosure.mjs` が `content/`・`src/`・`public/` を走査し、
禁止ワードの混入を検出する。pre-commit hook からも自動実行される。

禁止ワードのリストは**リポジトリの外**（既定 `/srv/agent-policy/denylist.txt`）にある。
リスト自体が取引先名の一覧であり、公開できないため。

このチェックは**最終防衛線であって、判断の代わりにはならない**。
リストに無い語でも、上の規約に反するものは書いてはいけない。
リストは完全ではなく、新しい案件名が出てくれば追加が必要になる。

## 非公開ソースをリポジトリに持ち込まない

`/srv/silverbullet*` の内容をこのリポジトリにコピーしないこと。
一時ファイルとしても置かない。読むのは元の場所から直接行う。
